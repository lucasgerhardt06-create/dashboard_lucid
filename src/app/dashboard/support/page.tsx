import { DenseTable, PageHeader, ViewPanel } from "@/components/dashboard/analytics-ui"; import { readView } from "@/lib/views";
export const dynamic="force-dynamic"; export const revalidate=0;
export default async function SupportPage(){const feedback=await readView("v_feedbacks_support",{limit:100,order:"created_at"});return <><PageHeader title="Support" description="Boîte de tri des retours. Les diagnostics sont disponibles côté serveur dans la table feedbacks pour le détail."/><ViewPanel title="Retours récents" view="v_feedbacks_support" error={feedback.error} empty={!feedback.data.length}><DenseTable label="Retours support" rows={feedback.data} columns={[{key:"created_at",label:"Date"},{key:"ref",label:"Référence",emphasis:true},{key:"type",label:"Type"},{key:"content",label:"Contenu"},{key:"platform",label:"Plateforme"},{key:"ent_service",label:"Service"},{key:"app_version",label:"Version"}]}/></ViewPanel></>}
